[phases.setup]
nixPkgs = ["python311", "gcc"]

[start]
cmd = "cat app.py audit_snippet.py > app_runtime.py && streamlit run app_runtime.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true"
